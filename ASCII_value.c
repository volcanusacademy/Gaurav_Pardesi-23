#include <stdio.h>
// Program to print characters of all the ASCII values from 91 to 96

int main() {
    for (int i = 91 ; i<=96;i++){
        printf("ASCII value of %d = %c\n" ,i , i);
    }

    return 0;
}