#include <stdio.h>
//  Program to display alphabets using ASCII values
int main() {
    int num ;
    printf("Enter number: ");
    scanf("%d",&num);
    printf("ASCII alphabet of %d is %c",num,num);
    return 0;
}