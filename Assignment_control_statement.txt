#include <stdio.h>

int main()
{   int number;
    printf("Enter the value: ");
    scanf("%d",&number);
    if (number>0){
        printf(" %d is positive number",number);
    }
    else if (number<0){
            printf(" %d is Negative number",number);
    }
    else{
         printf(" %d is not Negative and postive ",number);
    }

    return 0;
}
